dist
dev-dist
coverage
pnpm-lock.yaml

# Supabase CLI ephemeral cache (already in supabase/.gitignore; Prettier
# doesn't auto-read nested .gitignore files, so it needs its own entry here).
supabase/.temp
supabase/.branches

# Generated by `supabase gen types` (T-0.5) — not hand-written.
src/lib/database.types.ts

# Deno Edge Functions (T-5.1) — a separate runtime/toolchain from the Node/
# Vite app; not worth coupling its formatting to this Prettier config.
supabase/functions/

# Out of scope for the Phase 0 toolchain gate: human-authored docs, SDD
# artifacts, diario notes, and deploy config with its own hand-tuned
# formatting (see CLAUDE.md's rich-HTML/humanizer doc conventions). Prettier
# here is scoped to the app + its JS/TS toolchain, not the whole repo.
docs/
diario/
openspec/
*.md
wrangler.jsonc
