# Diario — 2026-06-21

## Hecho

- **Replanteo de arquitectura.** Se descartó el API Python en Railway. Stack nuevo:
  SPA (Vite + React 19 + TS + Tailwind 4 + PWA) → Supabase (São Paulo) →
  Cloudflare Pages. Inspirado en `iot-assistant` pero podado: sin el FastAPI de
  visión/telemetría y sin TimescaleDB (Antimahue no tiene ni imágenes-IA ni series
  temporales). Sin Docker en producción.
- **Seguridad como principio rector.** El proyecto maneja dinero y datos de terceros
  → la seguridad va en el centro de cada fase: RLS deny-by-default, cliente no
  confiable (autorización en Postgres, no en el JS), venta atómica, audit log, PIN
  endurecido (rate-limiting), separación de claves anon/service_role, pnpm v11.
- **`CLAUDE.md`** creado (guía del repo).
- **SDD `setup-stack` completo**: proposal → spec → design → tasks (todas
  `completed`), con companions `.html` de presentación. `project.yaml` actualizado.
- **Apply batch 1** (sin credenciales): scaffold de la SPA (estructura atomic +
  features + lib + stores), config de pnpm/PWA y `_headers` (CSP) de Cloudflare.
  **Build verde** (PWA generada). El build-gate cazó 2 bugs de tipos que se
  arreglaron: `crypto.ts` (`Uint8Array<ArrayBuffer>` por TS 5.7) y la falta de
  `src/vite-env.d.ts` (para `import.meta.env`).
- **Supabase**: proyecto creado en São Paulo con deny-by-default (expose-new-tables
  OFF + automatic RLS ON), repo conectado (schema-as-code). El token de acceso del
  `.envrc` estaba revocado → resuelto con un PAT nuevo (ref `aruteznqhdaaxxvllvzm`,
  `ACTIVE_HEALTHY`).
- **Latencia medida desde Chile**: São Paulo (~70 ms) le gana a USA (~150 ms); el
  edge de CF y Vercel empata (~17 ms).
- **`docs/Ukryl/`** gitignoreado: destilación WIP de PDFs del proveedor Ukryl, seed
  para el futuro change `data-model`.
- **Reconciliación `/sync-memoria` + `/cierre-sesion`.** `CLAUDE.md` de-staled (decía
  "no hay código / stack sin cerrar" — falso, el scaffold existe y buildea verde),
  `setup-stack/state.yaml` → `apply` in_progress, `tasks.md` check-off sincronizado
  **23/48** (verificado contra filesystem, no a ciegas). Engram: obs apply-batch-1
  (id 1492) + supabase-token-fix (id 1494).
- **Fix del MCP de Supabase.** ROOT CAUSE del `Unauthorized`: el token VIEJO revocado
  estaba **hardcodeado en `argv`** de `~/.claude.json` (`--access-token`), pisando el
  `.envrc` — confirmado por fingerprint sha256 + curl (viejo→401, nuevo del env→200).
  Fix env-based aplicado (flag removido, hereda del entorno; backup en
  `~/.claude.json.bak-presync-supabase`).

## Pendientes

- [ ] **Verificar el MCP de Supabase tras relanzar Claude.** Fix ya aplicado (ver
      Hecho). Pendiente: relanzar Claude desde el workspace (direnv activo) y
      confirmar con `list_projects`. Riesgo: Claude puede reescribir `~/.claude.json`
      al salir y clobbear el edit → re-aplicar del backup si vuelve el `Unauthorized`.
- [ ] **Apply batch 2**: escribir el SQL del scaffold de seguridad (`profiles` +
      trigger de signup + RLS deny-by-default + `audit_log`) y aplicarlo vía MCP
      `apply_migration`.
- [ ] Correr `get_advisors` (seguridad) tras las migraciones.
- [ ] Optimizar el chunk principal (>500 kB) con code-splitting.
- [ ] Smoke-test de WebCrypto + IndexedDB en la PWA instalada (tarea 7.10).
- [ ] Decidir cuándo commitear (**24 archivos** sin commitear; nada commiteado hoy).
      Las tareas 1.10 (commit `pnpm-lock`), 6.5 y 7.8 dependen de esto.
- [ ] Arreglar `.env.example`: es un stub roto (`# Rename to .env.example`, le faltan
      `VITE_SUPABASE_URL=` / `VITE_SUPABASE_ANON_KEY=` — tarea 2.3).
- [ ] `data-model`: change futuro (usa el seed de Ukryl con validación humana — la
      extracción de PDF es lossy).

> Resueltos hoy (movidos de Pendientes): `allowBuilds` ya verificado (va en
> `pnpm-workspace.yaml`, no en `.npmrc`, en pnpm v11); check-off de `tasks.md`
> sincronizado 23/48.

## Notas

- El plugin MCP de Supabase usa `SUPABASE_ACCESS_TOKEN` (no OAuth): un token
  revocado rompe el CLI y el MCP a la vez.
- El CLI de Supabase NO necesita Docker para el flujo cloud (`link`/`db push`);
  Docker solo es para `supabase start` (stack local), que este proyecto no usa.
- Dos sub-agentes de apply murieron por `API 529` (overload); las operaciones
  locales (build, fixes, curl) esquivan el 529 porque no son llamadas al modelo.
