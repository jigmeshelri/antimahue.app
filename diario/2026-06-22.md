# Diario — 2026-06-22

Continuación de la sesión del 21. El change `setup-stack` pasó de 30/48 a **48/48**,
verificado end-to-end y **desplegado en producción** con dominio propio.

## Hecho

- **Batch 2 — scaffold de seguridad de la DB.** Verify `PASS_WITH_WARNINGS` (0
  CRITICAL). Las alarmas iniciales (modelo de roles, `rls_auto_enable`, RLS sin
  policy) se disolvieron con evidencia. **Gotcha resuelto:** `apply_migration` vía
  MCP registró la versión con su timestamp de ejecución (`20260622015914`) ≠ prefijo
  del archivo (`20260621000000`); el CLI de CF/Supabase matchea por `version` (PK) →
  el push habría roto el auto-deploy. Fix: `UPDATE schema_migrations` (repair) vía
  `execute_sql`. Commiteado + pusheado.
- **Batch 3.** W2 documentado en `design.md`/`.html` (los 2 advisor WARN sobre
  `rls_auto_enable` son falsos positivos: es un `event_trigger` inalcanzable —
  NO revocar). `wrangler.jsonc` creado. `pnpm audit` limpio. **Decisión V-7:** anon
  `GET /profiles` → `401` (no `[]`) es deny-by-default a **dos capas** (sin grant DML
  + RLS sin policy), más estricto que el spec original; spec ajustado; los grants
  mínimos + policies se difieren a `data-model`. Final verify `PASS_WITH_WARNINGS`.
- **Migración a publishable key.** De la legacy anon key a `sb_publishable_` en
  `supabase.ts` + `.env.example` + artefactos SDD. El **rol** Postgres `anon`
  (RLS/grants/401) quedó intacto — solo cambió el nombre de la **key** del cliente.
  Verificada end-to-end (la key real conecta a Supabase; `401/42501` = key válida +
  deny-by-default).
- **Aceptación verificada a runtime.** 7.10 smoke-test: round-trip de `crypto.ts`
  (PBKDF2 + AES-GCM) + IndexedDB → PASS en browser real. 7.2/7.3: la app conecta a
  Supabase con la publishable key. 2.2: `.env.local` con la key real.
- **Deploy a Cloudflare.** El primer deploy falló: el dashboard nuevo de CF corre
  `wrangler deploy` (Workers Builds), no Pages clásico → `pages_build_output_dir` no
  sirve. Fix: `wrangler.jsonc` a **Workers Static Assets** (`assets.directory: ./dist`
  + `not_found_handling: single-page-application`). El `_headers` (CSP/HSTS) se
  respeta en ese modelo (confirmado por doc y por `curl`). **LIVE y verificado** en
  `*.workers.dev` y en el dominio propio **`antimahue.com`** (HTTP 200, CSP/HSTS/
  X-Frame, SPA fallback, `<title>Antimahue POS</title>`).
- **`setup-stack` 48/48.** Working tree limpio, todo pusheado (HEAD `5e3a5a2`).

## Pendientes

- [x] **`sdd-archive` del `setup-stack`** — HECHO (en sesión posterior, junto al
      `/sync-memoria`). Spec consolidada → `openspec/specs/setup-stack/spec.md`; change
      movido a `openspec/changes/archive/2026-06-22-setup-stack/`; `state.yaml` →
      `archived`; `setup-stack` fuera de `active_changes` (queda solo
      `color-palette-assistant`). Commits `64c75b8` (sync) + `fe747be` (archive).
- [ ] `www.antimahue.com` no responde — configurar en el dashboard de CF (Custom
      Domain del Worker o Redirect Rule `www → apex`). Opcional, no toca el repo.
- [ ] Code-splitting del chunk principal (>500 kB).
- [ ] Change futuro `data-model`: RLS policies de empleado + `GRANT SELECT` mínimos +
      RPC de venta atómica + seed Ukryl.

## Notas

- **`VITE_*` son build-time**: Vite las embebe en el bundle público (se ven en el JS
  de producción). NO son secretos; encriptarlas en CF no cambia eso. La publishable
  key es segura en el cliente *con RLS*. La `service_role` jamás lleva prefijo `VITE_`.
- **CF migra Pages → Workers Static Assets**: para SPA estático usar `assets.directory`
  + `not_found_handling: single-page-application`; el `_headers` se respeta.
- La CSP se escribe con `'self'` (no dominio hardcodeado) → se adaptó sola a
  `antimahue.com` sin tocar `_headers`.
