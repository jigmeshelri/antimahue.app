# Diario — 2026-07-05

Sesión de infraestructura y fachada: el repo pasó de "docs que mienten y ruleset
roto" a workflow PR-only operativo con commits firmados, y estrenó README público.

## Hecho

- **sync-memoria.** El archive de `setup-stack` (2026-06-22) confirmado real;
  CLAUDE.md todavía decía "sdd-archive pendiente" → corregido (header, estado,
  tabla de changes activos). AGENTS.md decía "repositorio vacío" desde el
  2026-05-30 → refresh completo a la realidad live. PR #11.
- **PR #10 mergeado** (desde el dashboard): `wrangler.jsonc` name →
  `antimahue-app`, matchea el Worker deployado; se elimina el riesgo de crear un
  Worker duplicado con un `wrangler deploy` local.
- **Ruleset "Default" de GitHub.** Bug encontrado: `main,develop` tipeado en UN
  solo campo = pattern fnmatch literal → el ruleset no protegía nada. Arreglado
  por API. Estado final: PR obligatorio (0 approvals), sin force-push, sin
  borrado, firmas requeridas — sobre `main` y `develop`. Verificar rulesets por
  API después de crearlos: la UI acepta patterns basura sin avisar.
- **Workflow PR-only adoptado** ("me ordena la cabeza"): nada de push directo a
  `main`, aunque el equipo sea de uno. Documentado en CLAUDE.md + AGENTS.md y
  ejercitado con los PRs #11–#14 el mismo día del estreno.
- **Firma SSH de commits.** Config repo-local (`id_ed25519_j`; no global — la
  otra key de la máquina es de trabajo). `required_signatures` mordió en vivo
  (el merge de PR #11 necesitó `--admin`), se quitó del ruleset, se registró la
  Signing Key en GitHub y se reactivó: PR #13 mergeó firmado sin bypass.
  Gotchas: la verificación de GitHub es retroactiva (commits firmados antes de
  registrar la key quedan `verified` después); el rebase-merge recrea commits
  sin firma → mergear por merge-commit o squash.
- **README público de presentación** (PR #14): logo SVG construido desde el
  design handoff (hoja de maple, gradiente `#C84030→#8A2010`), badges de shields
  teñidos con la paleta Terraza (fondo corteza, logos ámbar), la historia del
  producto antes que el stack, y cero credenciales (el repo es público). De
  yapa: description + homepage del repo en GitHub actualizados.

## Pendiente

- `color-palette-assistant`: fase `specs` (proposal completo desde el 2026-06-20).
- Suite de tests (sigue sin existir `pnpm test` — no inventarlo).
- Rama `develop`: crear recién cuando haya equipo/integración (el ruleset ya la cubre).
