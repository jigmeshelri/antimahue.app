# Diario — 2026-08-18

Sync-memoria de rutina post-cierre de `venta`. No había sesión de trabajo de producto nueva; el objetivo fue verificar que `AGENTS.md`, `CLAUDE.md` y Engram reflejen el estado real del repo.

## Hecho

- **Diagnóstico Engram:** `engram doctor --project antimahue.app` → 4 ok / 0 warnings / 0 blocked. El namespace plano `antimahue.app` sigue consolidado (92 obs, 4 sessions).
- **Drift detectado en docs.** `AGENTS.md` y `CLAUDE.md` estaban congelados en el estado del 2026-08-06:
  - Decían "tres changes archivados" cuando ya son cuatro (`data-model`, `auth-pin`, `catalogo`, `venta`).
  - Marcaban `venta` como próximo change; en realidad fue implementado, verificado, mergeado (PRs #44/#45) y archivado el 2026-08-13.
  - Describían `color-palette-assistant` como "proposal completo, specs pending"; el `state.yaml` real indica `design: completed` y `tasks: pending`.
  - El test count decía `166 passed | 7 skipped`; la suite actual reporta `233 passed | 7 skipped`.
- **Docs sincronizados.** Se actualizaron `AGENTS.md` y `CLAUDE.md` para reflejar:
  - Estado al 2026-08-18.
  - Cuatro changes archivados y `color-palette-assistant` como change activo.
  - Párrafo descriptivo del change `venta` (carrito, confirmación, deshacer, ticket térmico).
  - Test count correcto.
- **Suite verificada.** `pnpm test` → 233 passed, 7 skipped (CI parity). Los 7 skipped son la batería RLS multi-rol local-only.
- **Post-pull (cambios bajados de otra máquina).** Después del sync inicial el usuario corrió `git pull` y bajó commits que estaban pendientes en otra máquina:
  - `diario/2026-08-11.md`: sesión de implementación del apply de `venta` en `feat/venta` (8 commits, sin PR todavía en ese momento).
  - `openspec/changes/color-palette-assistant/tasks.md`: breakdown de 5 fases y 26 tareas para el change activo.
  - Limpieza de la carpeta stale `openspec/changes/venta/` tras el archive del 2026-08-13.
  - El pull no generó conflictos con los cambios locales de sync-memoria (`AGENTS.md`, `CLAUDE.md`, `diario/2026-08-18.md`). `color-palette-assistant` sigue en `design: completed` / `tasks: pending` según `state.yaml`.

## Aprendido

- El drift más reciente fue de documentación, no de memoria Engram. Después de un change grande como `venta`, los archivos de agente en el repo envejecen rápido si no se revisan explícitamente.
- `engram doctor` no detecta drift de Markdown del repo; ese chequeo sigue siendo trabajo del sync-memoria.

## Pendiente

Los pendientes del change `venta` y sesiones anteriores siguen vigentes salvo que se indique lo contrario:

- [ ] **(MED)** W-3 — trigger companion `AFTER UPDATE OF raw_app_meta_data` para provisionar un segundo admin sin la race de GoTrue.
- [ ] **(MED)** W-4 — habilitar `auth_leaked_password_protection` en Supabase Auth.
- [ ] **(LOW)** W-1 — captura HAR/devtools real del desbloqueo por PIN.
- [ ] **(LOW)** W-5 — capturar preflight `OPTIONS` de CORS real desde el browser.
- [ ] **(LOW)** Confirmar con la usuaria el umbral de 5 minutos del auto-bloqueo.
- [ ] Verificar toggle "Allow new users to sign up" en el dashboard de Supabase.
- [ ] **(MED)** Escribir la regla de naming de *proyectos* en `~/.claude/skills/_shared/engram-convention.md`.
- [ ] **(MED)** Consolidar la flota MCL en Engram: Cotizadores (5 claves), OneTrust (4), 15cot (3).
- [ ] **(LOW)** Diagnosticar `sync_mutation_required_fields` en `blocked` a nivel flota.
- [ ] Issues #4 y #5 quedan abiertos con ACs residuales: fotos con Storage (#4) y fallback de escáner + consulta offline (#5).
- [ ] Change activo: **`color-palette-assistant`** (listo para fase `tasks`).
