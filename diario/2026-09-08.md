# Diario — 2026-09-08

Sync-memoria de arranque tras tres semanas sin sesiones (última: 2026-08-18). No hubo trabajo de producto nuevo; el objetivo fue reconciliar memoria, docs y estado real del backend.

## Hecho

- **Backend pausado.** El proyecto Supabase (`aruteznqhdaaxxvllvzm`) estaba en `status: INACTIVE`: auto-pause del free tier tras ~7 días sin uso (última actividad 2026-08-19). El frontend en `antimahue.com` seguía dando HTTP 200 porque es un bundle estático, pero login y datos fallaban. Sergio lo restauró desde el dashboard de Supabase.
- **Migraciones verificadas post-restore.** `list_migrations` y `supabase_migrations.schema_migrations` devuelven 9 versiones, idénticas 1:1 a `supabase/migrations/` (de `20260621000000_initial_scaffold` a `20260818000000_resumen_dashboard_rpc`). `public.resumen_dashboard()` existe como `SECURITY DEFINER`: la GitHub integration sí aplicó la migración del dashboard al mergear el PR #47.
- **Sesión fantasma detectada: `dashboard`.** El ciclo SDD completo (PRs #46/#47/#48, mergeados 2026-08-18/19, verify PASS 271 tests, archive en `openspec/changes/archive/2026-08-18-dashboard/`) no tenía observaciones en Engram ni entrada de diario. Mismo patrón que `catalogo` el 2026-08-06.
- **Docs sincronizados.** `CLAUDE.md` y `AGENTS.md` pasan de "cuatro changes" a cinco, con párrafo descriptivo de `dashboard`, test count `271 passed | 7 skipped` (278 con la batería RLS) y el gotcha del auto-pause.
- **`color-palette-assistant`:** `state.yaml` ahora refleja `tasks: completed` (el `tasks.md` con 22 tareas en 5 fases existía desde el 2026-08-13 pero el estado seguía en `pending`). Próxima fase: `apply`.
- **Issue #8 (dashboard):** sigue abierto aunque el change está archivado; el cierre automático fue denegado por el sandbox y queda como acción manual (ver Pendiente).
- **`.atl/` gitignoreado** (caché local del skill-registry que apareció hoy).
- `main` local estaba 3 commits atrás de `origin/main`; fast-forward sin conflictos.

## Aprendido

- Mientras Supabase está en `COMING_UP`, `list_migrations` devuelve lista **vacía**, no error. No leerlo como "sin migraciones": esperar `ACTIVE_HEALTHY` y reconsultar. El REST responde 401 (sin apikey) antes de que Postgres acepte TCP en 5432.
- El auto-pause va a repetirse mientras el proyecto sea free tier y Angélica no lo use a diario. Chequear `get_project.status` al inicio de cada sesión es parte del sync-memoria de este repo.

## Pendiente

Los pendientes de sesiones anteriores siguen vigentes salvo que se indique lo contrario:

- [ ] **(MED)** W-3 — trigger companion `AFTER UPDATE OF raw_app_meta_data` para provisionar un segundo admin sin la race de GoTrue.
- [ ] **(MED)** W-4 — habilitar `auth_leaked_password_protection` en Supabase Auth.
- [ ] **(LOW)** W-1 — captura HAR/devtools real del desbloqueo por PIN.
- [ ] **(LOW)** W-5 — capturar preflight `OPTIONS` de CORS real desde el browser.
- [ ] **(LOW)** Confirmar con la usuaria el umbral de 5 minutos del auto-bloqueo.
- [ ] Verificar toggle "Allow new users to sign up" en el dashboard de Supabase.
- [ ] **(LOW)** Bundle `index` ~509 kB supera el límite de 500 kB de Vite (warning no bloqueante desde `dashboard`); evaluar code-splitting por ruta.
- [ ] Decidir si conviene un ping periódico o pasar a plan pago para evitar el auto-pause de Supabase.
- [ ] Issues #4 y #5 quedan abiertos con ACs residuales: fotos con Storage (#4) y fallback de escáner + consulta offline (#5).
- [ ] Cerrar issue #8 (dashboard) con referencia a `openspec/changes/archive/2026-08-18-dashboard/` — el change ya está implementado, verificado y archivado.
- [ ] Change activo: **`color-palette-assistant`** (listo para fase `apply`).
