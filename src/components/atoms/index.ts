/**
 * Atomic UI primitives — barrel.
 * Implemented: PinDot, PinKey, AppIcon (Phase 4, T-4.1–T-4.3).
 * Still placeholders for a future UI change: Button, Input, Badge.
 */
export { default as PinDot } from './PinDot'
export { default as PinKey } from './PinKey'
export { default as AppIcon } from './AppIcon'
