/**
 * Molecule UI components — barrel.
 * Implemented: PinDots, PinPad, UserSelector (Phase 4, T-4.4–T-4.6).
 * Still placeholders for a future UI change: SearchBar, ProductCard, KpiCard.
 */
export { default as PinDots } from './PinDots'
export { default as PinPad } from './PinPad'
export { default as UserSelector } from './UserSelector'
