/**
 * DteImportScreen — dte feature (Screen 9).
 * Skeleton per tasks.md 4.8.
 * XML parse client-side or via Edge Function; RUT is PII (A2, T2).
 */
export default function DteImportScreen() {
  return <div>DteImportScreen</div>
}
