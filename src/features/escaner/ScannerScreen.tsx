/**
 * ScannerScreen — escaner feature (Screen 4).
 * Skeleton per tasks.md 4.8.
 * Will use Barcode Detection API (future escaner change).
 */
export default function ScannerScreen() {
  return <div>ScannerScreen</div>
}
