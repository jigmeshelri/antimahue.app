/**
 * SupplierScreen — proveedor feature (Screen 8).
 * Skeleton per tasks.md 4.8.
 * Handles supplier PII — RLS-protected (T2).
 */
export default function SupplierScreen() {
  return <div>SupplierScreen</div>
}
